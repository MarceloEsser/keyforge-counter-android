<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/base_line_schedule_navigation"
    app:startDestination="@id/login_fragment"
    tools:ignore="UnusedNavigation,UnusedIds">

    <include app:graph="@navigation/navigation_play_maker" />

    <fragment
        android:id="@+id/login_fragment"
        android:name="keyforge.counter.android.login.LoginFragment"
        android:label="@string/login_fragment_label">
        <action
            android:id="@+id/go_to_fragment_counter"
            app:destination="@id/navigation_play_maker"/>
    </fragment>
</navigation>
